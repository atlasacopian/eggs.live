// Add inStock field to la_egg_prices model
model la_egg_prices {
  id               Int             @id @default(autoincrement())
  store_location_id Int
  store_location   store_locations @relation(fields: [store_location_id], references: [id])
  price            Float
  date             DateTime
  eggType          String          @default("regular")
  inStock          Boolean         @default(true)  // New field

  @@unique([store_location_id, date, eggType])
  @@index([date])
}

// Also update the national egg_prices model
model egg_prices {
  id               Int             @id @default(autoincrement())
  store_location_id Int
  store_location   store_locations @relation(fields: [store_location_id], references: [id])
  price            Float
  date             DateTime
  eggType          String          @default("regular")
  inStock          Boolean         @default(true)  // New field

  @@unique([store_location_id, date, eggType])
  @@index([date])
  @@index([store_location_id])
}

